{
  "name": "alpaca",
  "type": "module",
  "version": "1.0.0",
  "description": "You may need to use sudo with that command.  After installing the python3-venv",
  "main": "./dist/esm/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "npm run build && node dist/esm/index.js",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ollama": "^0.5.8",
    "torc": "github:gatlin/torc",
    "ts-functional-pipe": "^3.1.2",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "rimraf": "^6.0.1",
    "tsc": "^2.0.4"
  },
  "bin": {
    "alpaca": "./dist/esm/index.js"
  },
  "private": true
}
